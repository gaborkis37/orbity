# Public base URL of the Orbity API (read by the browser — must be NEXT_PUBLIC_*)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Initial catalog and Web Worker propagation cadence
NEXT_PUBLIC_SATELLITE_GROUP=starlink
NEXT_PUBLIC_PROPAGATION_TICK_MS=1000
